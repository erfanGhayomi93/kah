'use client';

import Analyze from '@/components/common/Analyze';

const StrategyAnalyzer = () => {
	return <Analyze contracts={[]} baseAssets={2e4} useCommission />;
};

export default StrategyAnalyzer;
