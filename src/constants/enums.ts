export enum Environment {
	DEV = 'dev',
	STAGE = 'stage',
	PREPROD = 'preprod',
	PROD = 'production',
}

export enum DateAsMillisecond {
	Day = 864e5,
	Week = 6048e5,
	Month = 2592e6,
	Year = 31536e6,
}
